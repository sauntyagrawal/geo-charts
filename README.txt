README

Please change the value appUrl in myscript.js before deploying.

Example:
 
If the path of your application is http://localhost:8080/App/data/getTrends.jsp

Then set appUrl as follows

var appUrl = "http://localhost:8080/App" ;

(Don't use trailing / character)